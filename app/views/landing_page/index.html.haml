Hello this is the landing page beep

%header
  %div
    - if user_signed_in?
      = link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete}
    - else
      = link_to "Sign in", new_user_session_path
      = link_to "Register", new_user_registration_path
