%h2.title Contact Us

%div.page
  %p If you want to place a special order, use the form below.

  %div.contact-form
    = simple_form_for @contact do |f|
      = f.input :email
      = f.input :phone
      = f.input :address
      = f.input :message
      = f.button :submit
